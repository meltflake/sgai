---
// About page (zh) — thin wrapper. Body lives in `src/components/about/AboutPage.astro`.

import Layout from '~/layouts/PageLayout.astro';
import AboutPage from '~/components/about/AboutPage.astro';

const lang = 'zh' as const;

const metadata = {
  title: '关于本站',
  description: '关于新加坡 AI 观察——独立维护的新加坡 AI 战略观察平台。研究方法、利益声明、反馈方式。',
};
---

<Layout metadata={metadata} breadcrumbs={[{ label: '关于本站' }]} lang={lang}>
  <AboutPage lang={lang} />
</Layout>
