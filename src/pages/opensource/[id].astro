---
import type { GetStaticPaths } from 'astro';
import Layout from '~/layouts/PageLayout.astro';
import OpenSourceProjectDetail from '~/components/data/OpenSourceProjectDetail.astro';
import { openSourceProjects } from '~/data/opensource';

export const prerender = true;

export const getStaticPaths = (() => {
  return openSourceProjects.map((project) => ({
    params: { id: project.id },
    props: { project },
  }));
}) satisfies GetStaticPaths;

const { project } = Astro.props;
---

<Layout
  metadata={{
    title: `${project.name} · 官方开源与研究`,
    description: project.summary || project.description,
  }}
  breadcrumbs={[{ label: '官方开源与研究', href: '/opensource/' }, { label: project.name }]}
>
  <OpenSourceProjectDetail project={project} lang="zh" />
</Layout>
