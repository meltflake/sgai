---
import { Icon } from 'astro-icon/components';

import Layout from '~/layouts/PageLayout.astro';
import Headline from '~/components/ui/Headline.astro';
import { ecosystemStats, exits, investors, unicorns, verticals } from '~/data/startups';
import { getStartupAiRelation, startupEntitySlug } from '~/utils/entity-pages';

const hrefFor = (entity: { id?: string; name: string; nameEn?: string }) => `/startups/${startupEntitySlug(entity)}/`;

const stats = [
  { value: ecosystemStats.totalStartups, label: 'AI 相关创业样本' },
  { value: `全球第 ${ecosystemStats.globalRank}`, label: 'AI 枢纽排名' },
  { value: ecosystemStats.seaFundingShare, label: '东南亚 AI 融资占比' },
  { value: ecosystemStats.totalVCRaised, label: 'VC 融资总额' },
  { value: String(ecosystemStats.unicorns), label: '独角兽' },
  { value: ecosystemStats.govCommitment, label: '政府 AI 承诺投入' },
];
---

<Layout
  metadata={{
    title: 'AI 创业生态',
    description: '新加坡 AI 创业与相邻生态——650+ 相关样本、融资数据、独角兽、垂直领域与投资人一览。',
  }}
  breadcrumbs={[{ label: 'AI 创业生态' }]}
>
  <section class="px-4 py-16 sm:px-6 mx-auto lg:px-8 max-w-6xl">
    <Headline
      title="AI 创业生态"
      subtitle="新加坡 AI-native、AI-enabled 与相邻资本生态的实体目录；每家公司、项目与投资者都进入可持续扩展的档案。"
    />

    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4 mb-12">
      {
        stats.map((stat) => (
          <div class="bg-surface rounded-lg shadow border border-subtle p-4 text-center">
            <div class="t-stat text-primary">{stat.value}</div>
            <div class="text-xs text-muted mt-1">{stat.label}</div>
          </div>
        ))
      }
    </div>

    <section class="mb-14">
      <div class="flex items-end justify-between gap-4 mb-5">
        <div>
          <p class="text-xs uppercase tracking-wider text-muted font-semibold mb-2">Scale Signals</p>
          <h2 class="t-h3">独角兽与上市样本</h2>
        </div>
        <span class="text-sm text-muted">{unicorns.length} 家</span>
      </div>
      <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
        {
          unicorns.map((u) => (
            <a
              href={hrefFor(u)}
              class="group h-full bg-surface rounded-lg shadow-sm border border-subtle p-5 hover:border-primary hover:shadow-md transition-all"
            >
              <div class="flex items-start justify-between gap-3 mb-4">
                <div>
                  <p class="text-xs text-muted mb-1">独角兽</p>
                  <h3 class="text-lg font-bold text-gray-900 dark:text-white group-hover:text-primary transition-colors">
                    {u.name}
                  </h3>
                  <span class="mt-2 inline-flex text-xs font-medium bg-primary/10 text-primary px-2 py-0.5 rounded">
                    {getStartupAiRelation(startupEntitySlug(u)).label}
                  </span>
                </div>
                <Icon name="tabler:arrow-up-right" class="w-5 h-5 text-muted group-hover:text-primary shrink-0" />
              </div>
              <p class="text-sm text-gray-700 dark:text-gray-300 leading-relaxed mb-4">{u.sector}</p>
              <dl class="grid grid-cols-2 gap-3 text-xs">
                <div>
                  <dt class="text-muted mb-1">融资</dt>
                  <dd class="font-semibold text-gray-900 dark:text-white">{u.raised}</dd>
                </div>
                <div>
                  <dt class="text-muted mb-1">估值</dt>
                  <dd class="font-semibold text-primary">{u.valuation}</dd>
                </div>
                <div class="col-span-2">
                  <dt class="text-muted mb-1">状态</dt>
                  <dd>{u.status}</dd>
                </div>
              </dl>
            </a>
          ))
        }
      </div>
    </section>

    <section class="mb-14">
      <div class="mb-6">
        <p class="text-xs uppercase tracking-wider text-muted font-semibold mb-2">Vertical Map</p>
        <h2 class="t-h3">按领域分类</h2>
      </div>
      <div class="space-y-10">
        {
          verticals.map((v) => (
            <section>
              <div class="flex items-center justify-between gap-4 mb-4">
                <h3 class="text-lg font-bold text-gray-900 dark:text-white">
                  {v.icon} {v.name}
                </h3>
                <span class="text-sm text-muted">{v.startups.length} 个实体</span>
              </div>
              <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
                {v.startups.map((s) => (
                  <a
                    href={hrefFor(s)}
                    class="group h-full bg-surface rounded-lg shadow-sm border border-subtle p-5 hover:border-primary hover:shadow-md transition-all"
                  >
                    <div class="flex flex-wrap items-center gap-2 mb-3">
                      <h4 class="font-bold text-gray-900 dark:text-white group-hover:text-primary transition-colors">
                        {s.name}
                      </h4>
                      {s.highlight && (
                        <span class="text-xs font-medium bg-primary/10 text-primary px-2 py-0.5 rounded">
                          {s.highlight}
                        </span>
                      )}
                      <span class="text-xs font-medium bg-gray-100 dark:bg-slate-800 text-gray-700 dark:text-gray-300 px-2 py-0.5 rounded">
                        {getStartupAiRelation(startupEntitySlug(s)).label}
                      </span>
                    </div>
                    <p class="text-sm text-gray-700 dark:text-gray-300 leading-relaxed mb-4">{s.description}</p>
                    <div class="flex flex-wrap gap-2 text-xs text-muted">
                      {s.raised && <span class="bg-gray-100 dark:bg-slate-800 px-2 py-1 rounded">融资 {s.raised}</span>}
                      {s.investors && (
                        <span class="bg-gray-100 dark:bg-slate-800 px-2 py-1 rounded">投资者 {s.investors}</span>
                      )}
                    </div>
                    <div class="mt-5 inline-flex items-center gap-1 text-sm font-medium text-primary">
                      查看档案
                      <Icon name="tabler:arrow-right" class="w-4 h-4" />
                    </div>
                  </a>
                ))}
              </div>
            </section>
          ))
        }
      </div>
    </section>

    <section class="mb-14">
      <div class="flex items-end justify-between gap-4 mb-5">
        <div>
          <p class="text-xs uppercase tracking-wider text-muted font-semibold mb-2">Exit Signals</p>
          <h2 class="t-h3">重要退出与收购</h2>
        </div>
        <span class="text-sm text-muted">{exits.length} 条</span>
      </div>
      <div class="grid gap-4 md:grid-cols-2">
        {
          exits.map((e) => (
            <a
              href={hrefFor(e)}
              class="group h-full bg-surface rounded-lg shadow-sm border border-subtle p-5 hover:border-primary hover:shadow-md transition-all"
            >
              <div class="flex items-start justify-between gap-3 mb-3">
                <div>
                  <p class="text-xs text-muted mb-1">{e.year || '年份待补'}</p>
                  <h3 class="text-lg font-bold text-gray-900 dark:text-white group-hover:text-primary transition-colors">
                    {e.name}
                  </h3>
                  <span class="mt-2 inline-flex text-xs font-medium bg-gray-100 dark:bg-slate-800 text-gray-700 dark:text-gray-300 px-2 py-0.5 rounded">
                    {getStartupAiRelation(startupEntitySlug(e)).label}
                  </span>
                </div>
                <Icon name="tabler:arrow-up-right" class="w-5 h-5 text-muted group-hover:text-primary shrink-0" />
              </div>
              <p class="text-sm text-gray-700 dark:text-gray-300 leading-relaxed mb-4">{e.description}</p>
              <dl class="grid gap-3 sm:grid-cols-2 text-xs">
                <div>
                  <dt class="text-muted mb-1">收购方</dt>
                  <dd class="font-semibold text-gray-900 dark:text-white">{e.acquirer}</dd>
                </div>
                <div>
                  <dt class="text-muted mb-1">金额</dt>
                  <dd class="font-semibold text-primary">{e.amount}</dd>
                </div>
              </dl>
              {e.note && <p class="mt-4 text-xs leading-relaxed text-muted line-clamp-3">{e.note}</p>}
            </a>
          ))
        }
      </div>
    </section>

    <section class="mb-12">
      <div class="flex items-end justify-between gap-4 mb-5">
        <div>
          <p class="text-xs uppercase tracking-wider text-muted font-semibold mb-2">Capital Network</p>
          <h2 class="t-h3">投资者生态</h2>
        </div>
        <span class="text-sm text-muted">{investors.length} 家机构</span>
      </div>
      <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
        {
          investors.map((inv) => (
            <a
              href={hrefFor(inv)}
              class="group h-full bg-surface rounded-lg shadow-sm border border-subtle p-5 hover:border-primary hover:shadow-md transition-all"
            >
              <div class="flex items-start justify-between gap-3 mb-3">
                <div>
                  <span class="text-xs font-medium bg-primary/10 text-primary px-2 py-0.5 rounded">{inv.type}</span>
                  <span class="ml-2 text-xs font-medium bg-gray-100 dark:bg-slate-800 text-gray-700 dark:text-gray-300 px-2 py-0.5 rounded">
                    {getStartupAiRelation(startupEntitySlug(inv)).label}
                  </span>
                  <h3 class="text-lg font-bold text-gray-900 dark:text-white group-hover:text-primary transition-colors mt-3">
                    {inv.name}
                  </h3>
                </div>
                <Icon name="tabler:arrow-up-right" class="w-5 h-5 text-muted group-hover:text-primary shrink-0" />
              </div>
              <p class="text-sm mt-3">
                <span class="text-muted">投资记录：</span>
                {inv.stats}
              </p>
              <p class="text-sm">
                <span class="text-muted">重点领域：</span>
                {inv.focus}
              </p>
            </a>
          ))
        }
      </div>
    </section>

    <p class="mt-8 text-sm text-muted text-center">
      基础统计口径截至 {ecosystemStats.dataDate}，实体档案整理于 {
        ecosystemStats.profileUpdated
      }。部分为估算值，融资金额来源于公开报道，实际数字可能有出入。
    </p>
  </section>
</Layout>
