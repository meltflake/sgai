---
// About page (en) — thin wrapper. Body lives in `src/components/about/AboutPage.astro`.

import Layout from '~/layouts/PageLayout.astro';
import AboutPage from '~/components/about/AboutPage.astro';

const lang = 'en' as const;

const metadata = {
  title: 'About',
  description:
    "About Singapore AI Observatory — an independently-maintained research platform tracking Singapore's AI strategy. Methodology, conflict-of-interest disclosure, feedback channels.",
};
---

<Layout metadata={metadata} breadcrumbs={[{ label: 'About' }]} lang={lang}>
  <AboutPage lang={lang} />
</Layout>
