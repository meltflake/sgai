# i18n 规范（多语言开发与验收清单）

> 本规范定义 sgai 项目支持多语言（当前 zh + en + ja + zh-tw + ko 五语）的方式。新增页面、新增数据、改动共享组件，全部按本规范执行；提交前必须通过 `npm run check:dist` 验证。
>
> Last updated: 2026-05-23

## 0. 五语状态速览（2026-05-23 引入 zh-tw + ko）

| Lang | URL | HTML lang | 数据字段 | UI 字典 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `en` | `/` (no prefix) | `en` | `*En` 必填 | 280 keys 完整 | 全站 ✓ |
| `zh` | `/zh/` | `zh-CN` | 裸字段（默认） | 280 keys 完整 | 全站 ✓ |
| `ja` | `/ja/` | `ja` | `*Ja` 必填 | 280 keys 完整 | 全站 ✓ |
| `zh-tw` | `/zh-tw/` | `zh-Hant` | **不存储**（运行时 OpenCC s2twp 转换） | 仅 override（`zhTwDict`） | 全站 ✓ (1028 pages, 0 residue) |
| `ko` | `/ko/` | `ko` | `*Ko` 可选（数据回填进行中） | 280 keys 完整（haiku 翻译 seed） | 基础设施 ✓，数据字段待补 |

**zh-tw 不需要 `*ZhTw` 数据字段**——`pickLocalized` 命中 zh 兜底时自动跑 OpenCC s2twp（Simplified→Traditional with Taiwan idiom substitution: 软件→軟體、视频→影片、信息→資訊、程序→程式、鼠标→滑鼠）。详见 [src/i18n/opencc.ts](../src/i18n/opencc.ts)。零数据膨胀、零 drift bug、零 API 翻译成本。手动 override 走 `zhTwDict` 字典或 `*ZhTw` 字段（命中优先级高于 OpenCC）。

**ko 需要 `*Ko` 数据字段**——目前 UI 字典已 seed（280 keys 已翻译，源码内置）但数据层尚未回填。缺失 `*Ko` 时按 fallback 链 `ko → en → zh` 回落到英文，可读但非母语。批量回填脚本见第 9 节。

**OpenCC 边界 case**：`src/i18n/opencc.ts` 内置了 ~20 条 post-process 补丁（`后续→後續` 之类的常见 mmseg 分词错位）。新发现 OpenCC 误转的 case，往 `POST_DICT` 加一行即可。验证：`npx tsx -e "import { toTraditional } from './src/i18n/opencc'; console.log(toTraditional('你的字符串'));"`。

## 1. 站点基本架构

- 默认路由语言 `en`（不带前缀，`/`、`/policies/`），其他语言一律加前缀（`/zh/...`、`/ja/...`、`/zh-tw/...`、`/ko/...`）。
- 默认**内容**语言是 `zh` —— 数据文件裸字段（`title` / `description` ...）默认存中文。其他语言用 `*<Cap-Lang>` 兄弟字段：`titleEn`、`titleJa`、`titleKo`（zh-tw 不存数据，运行时 OpenCC 转换）。
- 动态路由页：`src/pages/[lang]/<route>/<file>.astro` 一次涵盖所有非默认 locale，`getStaticPaths` emit 来自 `NON_DEFAULT_ROUTE_LOCALES`。Markdown 博客文件用 `src/data/post/<slug>.md`（zh）与 `src/data/post/<lang>/<slug>.md`（其他语言）。
- 所有 i18n 工具函数集中在 [src/i18n/index.ts](../src/i18n/index.ts)：
  - `LOCALES: Lang[]` / `DEFAULT_LOCALE: Lang` / `ROUTE_DEFAULT_LOCALE: Lang` — 当前激活的语言、内容默认、路由默认
  - `getLangFromPath(pathname)` → 当前语言（不识别的前缀回落 default）
  - `localePrefix(lang)` → `''`（路由默认）或 `'/<lang>'`
  - `localizedHref(path, lang)` → 给绝对路径加 lang 前缀
  - `unprefixed(path)` → 去掉任意 lang 前缀，恢复 canonical 默认语言路径
  - `pickLocalized(record, baseKey, lang)` → **新形态（推荐）**，N 语言通用，zh-tw 自动跑 OpenCC
  - `pickLocalized(record, zhKey, enKey, lang)` → 旧形态（保留兼容，仅限 zh+en）
  - `channelLabel(channel, lang)` → SocialChannel 的显示文本
  - `t(lang, key)` → 字典查询（zh 字典是真值，其他语言缺失时按 fallback 链回落）
  - `IN_LANGUAGES: Record<Lang, string>` → BCP-47 codes for `<html lang>` + hreflang + JSON-LD `inLanguage`
- `Lang` 联合类型: `'zh' | 'en' | 'ja' | 'zh-tw' | 'ko'`。Lang code 本身就是 URL 段（kebab-case 适配带 region 的语言：`zh-tw`、未来 `pt-br` 等）。
- `siblingSuffix`: 按 hyphen 切分后每段首字母大写：`'zh-tw'` → `'ZhTw'`，`'ko'` → `'Ko'`。这就是 `titleZhTw` / `titleKo` 字段名的由来（虽然 zh-tw 当前不需要）。

ja 已于 v0.12.0 完成全站集成（~5,756 条 `*Ja` 字段回填 + 42 个 `/ja/` 页面 + 8 篇日语博客 + 11 条 refresh 管线自动产出 ja 翻译）。

zh-tw + ko 已于 2026-05-23 加入基础设施（v0.13.0），见第 0 节状态表。

### 1.1 新增第六门语言的流程

设你要加 `fr`（法语）：

1. [src/i18n/index.ts](../src/i18n/index.ts): 把 `'fr'` 加进 `Lang` 联合类型 + `LOCALES` 数组
2. 在 `IN_LANGUAGES` 加 `fr: 'fr'`，在 `FALLBACK_CHAINS` 加 `fr: ['fr', 'en', 'zh']`
3. 添加导出 `export const fr: Partial<Record<keyof typeof zh, string>> = { ... }` 字典；注册到 `DICTIONARIES`
4. [Metadata.astro](../src/components/common/Metadata.astro) 的 `OG_LOCALES` 加 `fr: 'fr_FR'`
5. [utils.ts](../src/utils/utils.ts) 的 `FORMATTERS` 加 `fr: new Intl.DateTimeFormat('fr-FR', ...)`
6. [Layout.astro](../src/layouts/Layout.astro) 的 `HTML_LANG` 不需要改（reuse `IN_LANGUAGES`）
7. [LanguageToggle.astro](../src/components/common/LanguageToggle.astro) 的 `LANG_LABELS` 加 `fr: 'Français'`
8. [LangBanner.astro](../src/components/common/LangBanner.astro) 的 `BANNER_MESSAGES` + `SWITCH_LABELS` 加 fr 入口
9. 复制 `scripts/i18n/build-ko-dict.ts` 改名为 `build-fr-dict.ts`，跑 `npx tsx scripts/i18n/build-fr-dict.ts > scripts/i18n/data/fr-dict.txt`；splice 进 `src/i18n/index.ts` 的 `fr` 字典
10. [scripts/i18n-check.mjs](../scripts/i18n-check.mjs) 的 `LANG_CONFIG` 加 `fr` 配置（foreign-script regex + allow patterns）
11. `npm run check && npm run build && npm run check:dist` 验证

要不要给 `fr` 数据字段（`*Fr`）取决于优先级。基础设施跑通后，`pickLocalized` 命中 fr fallback 链回落到 en/zh，可读但非母语；批量回填 `*Fr` 的脚本模板见第 9 节（参考 ko）。

### 1.2 zh-tw 特殊：运行时 OpenCC 转换

zh-tw 不走传统"加字段+回填"流程。架构上，`pickLocalized` 和 `t()` 命中 zh 兜底（FALLBACK_CHAINS: `'zh-tw': ['zh-tw', 'zh']`）时，自动对返回值跑 `toTraditional()`（[src/i18n/opencc.ts](../src/i18n/opencc.ts)），输出 Taiwan-idiom Traditional。

副作用：
- **零数据膨胀**：不需要 `*ZhTw` 兄弟字段
- **零 drift bug**：zh 改了 zh-tw 自动跟上
- **零 API 翻译成本**：OpenCC 是本地决定性词典
- **不完美的边界 case**：mmseg 分词偶尔出错（如 `球后续` 被切成 `球后/续`），靠 [opencc.ts](../src/i18n/opencc.ts) 里 `POST_DICT` 做 ~20 条后处理补丁兜底。发现新边界 case 加一行即可。

什么时候要手动 `*ZhTw` 覆盖？三种情况：
1. 品牌/专有名词（`新加坡 AI 观察` 不能机械转换）—— 在 `zhTwDict` 字典里覆盖
2. 标题 / 长描述里有跨地区语义差异 —— 数据文件加 `titleZhTw` 字段
3. OpenCC 误转的高曝光内容（首页 hero / nav）—— `zhTwDict` 优先级最高

## 2. 数据契约（双字段约定）

> 一句话：数据文件以中文为主语言，其他语言以可选 `*<Cap-Lang>` 兄弟字段并列存储；展示由页面 + `pickLocalized` 决定。

### 2.1 命名规则

- 默认语言（zh）字段用裸名：`title`、`description`、`summary`、`role`...
- 其他语言加 capitalized 后缀：`titleEn`、`titleJa`、`descriptionEn`、`descriptionJa`...
- 数组字段同样加后缀：`keyDebates: string[]` ↔ `keyDebatesEn?: string[]`。
- TypeScript 接口里非默认语言字段都标 `?` 可选。

```typescript
export interface KeyInsight {
  title: string;
  titleEn?: string;
  titleJa?: string;
  description: string;
  descriptionEn?: string;
  descriptionJa?: string;
  evidence: string;
  evidenceEn?: string;
  evidenceJa?: string;
  significance: 'high' | 'medium' | 'low';
}
```

### 2.2 例外：标题已是英文的字段

某些数据 `title` 字段本身存的是英文（例如国会辩论 Hansard 原始标题）。此时 `zhTitle` 是中文兄弟字段。这种"英文为主+中文翻译"的反向布局允许存在，但所有页面渲染时**仍然走 `pickLocalized`**，不要假设方向。

### 2.3 状态枚举值

`status: '已落实' | '进行中' | '仍在讨论'` 这种枚举型字符串，比较用原值（中文），但**展示给用户的字符串要在页面层做翻译映射**。具体可参考 `src/pages/en/debates/index.astro` 的 `statusLabels`。

如果将来要彻底干净，把枚举值换成 `'implemented' | 'in-progress' | 'under-discussion'`，再用页面层翻译。当前为兼容历史数据保持中文枚举值。

### 2.4 SocialChannel `label` / `labelEn`

- `label` 默认存 zh（如 `"官网"`、`"MDDI 官网"`、`"@joteo_ylm"`）。
- 若 `label` 含 CJK 字符，**必须**同时给 `labelEn`（英文等价物，如 `"Website"`、`"MDDI website"`）。
- 平台名（twitter、linkedin、facebook 等）由 `channelLabel()` 内置兜底，不需要逐条写。
- 例外：纯拉丁文标签（`@joteo_ylm`、`Newsletter`）不需要写 `labelEn`，渲染时直接复用。

### 2.5 生成型长文本（字幕、transcript、抓取正文）

- 生成型长文本也必须遵守同一套兄弟字段规则，不能因为来源是外部抓取就直接复用到所有语言页面。
- 默认语言字段仍然用裸名，例如视频 transcript 的 `paragraphs` 是中文；英文用 `paragraphsEn`。
- 如果抓到的来源是英文，要先存入 `paragraphsEn`，再翻译成 `paragraphs` 给中文页使用。
- 页面层必须按 lang 选择字段；中文页不得直接把英文 source transcript 当作正文渲染。
- 国会辩论详情页是强约束：英文原文必须直接展开；非英文页面必须额外显示完整译文。中文页展示 `paragraphs`（中文译文）+ `paragraphsEn`（英文原文），英文页只展示 `paragraphsEn`。
- 视频 transcript 有专门检查：`npm run check:video-transcripts`。它已经并入 `npm run check`。
- 国会辩论 transcript 有专门检查：`npm run check:debate-transcripts`。它已经并入 `npm run check`。

## 3. 页面渲染规则

### 3.1 顶层契约

- 每个 `src/pages/<lang>/...` 页面顶部声明 `const lang = '<lang>' as const;` 并把 `lang={lang}` 传给 `Layout`。
- 每个 zh 页面如果使用了任何接受 `lang` prop 的组件，也要传 `lang`（默认从路径推断，但显式传更稳）。
- 渲染数据字段时，**永远不要**直接 `{record.title}` 当默认语言用。统一用：
  - `pickLocalized(record, 'title', lang)` ← **新形态推荐**，自动按 lang 选 `title` / `titleEn` / `titleJa`
  - 简写 `record.titleEn || record.title` 仅在 EN sibling 几乎总存在时使用，且会限制于双语系统
  - 旧形态 `pickLocalized(record, 'title', 'titleEn', lang)` 保留兼容，但新代码不要写

### 3.2 链接

- 跳到内部页面时，永远过一层 `localizedHref(path, lang)`。
- 不要硬编码 `/en/` 前缀；不要硬编码 `/people/...` 这类裸路径。
- 例：`<a href={localizedHref(`/people/${p.id}/`, lang)}>...</a>`

### 3.3 共享组件（zh + EN 都使用）

放在 `src/components/` 下、不分 lang 的组件（Header、Footer、Logo、LangBanner、LanguageToggle、Breadcrumb、AuthorBio、TableOfContents、NextPrevPost、RelatedRail、blog/ListItem、blog/SinglePost），必须**自己**从 URL 推断 lang：

```astro
---
import { getLangFromPath } from '~/i18n';
const lang = getLangFromPath(new URL(Astro.url).pathname);
const isEn = lang === 'en';
---
```

然后所有展示文案、内部链接、字段读取都按 lang 走。

### 3.4 镜像页应是 thin wrapper（v0.6.0 起强制）

> 一句话：任何在多语言下结构相同的页面（首页、about、timeline、人物详情等），**结构必须只存在一份**。每个 lang 的 page 文件只负责 metadata + JSON-LD，body 渲染统一委托给共享组件。

**为什么**：v0.5.0 之前我们把"数据层去重 + 页面层独立"作为分工。结果首页设计迭代两次，EN 都没跟上——`zh` 写的是裸中文字符串、`en` 写的是 `t(lang, ...)`，两份文件长得就完全不像，diff 看不出结构漂移；`i18n-check.mjs` 只查"EN 有没有中文残留"，不查"两边结构是否一致"。漂移是必然，不是意外。

**怎么做**：把页面 body 抽到 `src/components/<area>/<PageName>.astro`，接收 `lang: Lang` prop。两个 page 文件都引入同一个组件，各自只填自己的 metadata。

参考实现 — 首页：

- 共享组件：[`src/components/home/HomePage.astro`](../src/components/home/HomePage.astro)
- ZH 包装：[`src/pages/index.astro`](../src/pages/index.astro)（~40 行）
- EN 包装：[`src/pages/en/index.astro`](../src/pages/en/index.astro)（~40 行）

包装文件模板：

```astro
---
import Layout from '~/layouts/PageLayout.astro';
import JsonLd from '~/components/common/JsonLd.astro';
import MyPage from '~/components/<area>/MyPage.astro';
import { t } from '~/i18n';

const lang = 'zh' as const; // or 'en' as const

const metadata = { title: t(lang, 'siteName'), description: t(lang, 'siteDescription') };
const websiteSchema = { '@context': 'https://schema.org', '@type': 'WebSite', /* ... */ };
---

<Layout metadata={metadata} lang={lang}>
  <JsonLd schema={websiteSchema} />
  <MyPage lang={lang} />
</Layout>
```

共享组件内部规则：

- 所有用户可见文案走 `t(lang, key)` 字典；不要嵌任何裸字符串
- 数据字段走 `pickLocalized(record, baseKey, lang)`
- 内部链接走 `localizedHref(path, lang)`
- 不要在共享组件里 `if (lang === 'zh') ... else ...` 分支结构差异——结构必须一致，差异只在文案

**适用范围**：

- ✅ 首页、about、timeline、tracker overview 这种镜像页
- ✅ 镜像但只是顺序/排版略有不同的页面——把差异收进字典或参数，仍然走共享组件
- ❌ Markdown 博客文章（`src/data/post/<slug>.md` 与 `src/data/post/en/<slug>.md`）——内容本就是各语言独立写的，沿用现有双文件方案
- ❌ EN 独占或 ZH 独占的页面——没有镜像，自然没漂移问题

**新增镜像页的步骤**：

1. 写共享组件 `src/components/<area>/<PageName>.astro`，body + 顶部 `interface Props { lang: Lang; }`
2. 在 zh 字典里把所有用户可见文案登记成 key，EN 字典补对应翻译
3. 在 `src/pages/<route>` 与 `src/pages/en/<route>` 各放一个 thin wrapper
4. 跑 `npm run build && npm run check:i18n` 确认 EN 端无中文残留

**改动镜像页时**：直接改共享组件。**永远不要**只改其中一个 page 文件。如果发现自己在 page 文件里写了任何 body 结构（不只是 metadata），停下来，把它抽到共享组件去。

### 3.5 zh 副字段（zhTitle, zhName, zhEvent, etc.）

- 在 zh 页面：英文条目可保留 zh 翻译做副显示，符合中文阅读体验。
- 在 EN 页面（以及未来的 ja/ko/...）：**完全隐藏** zh 副字段。EN 页面要保持纯英文，不展示中文注音/副标题。一旦在非默认语言页面渲染了 zh 副字段，i18n-check 会报错。
- 这是 v0.5.0 起的新约定（之前曾允许短姓名做注音，已撤销）。

## 4. SEO 与 Metadata

- `<html lang>`：zh 页面输出 `lang="zh-CN"`；EN 页面输出 `lang="en"`；JA 页面输出 `lang="ja"`。已由 `src/layouts/Layout.astro` 统一处理。
- `og:locale`：zh `zh_CN`、EN `en_US`、JA `ja_JP`。由 `src/components/common/Metadata.astro` 根据 lang 输出。
- `og:site_name`：zh `新加坡 AI 观察`、EN `Singapore AI Observatory`、JA `シンガポール AI 観測`。来源是 `src/i18n/index.ts` 字典。
- `<title>` 模板：zh `%s — 新加坡 AI 观察`、EN `%s — Singapore AI Observatory`、JA `%s — シンガポール AI 観測`。
- `description` 兜底：每个页面应自带各语言 description；缺失时回落 `t(lang, 'siteDescription')`。
- `<link rel="alternate" hreflang>`：所有页面自动输出 `en` + `zh-CN` + `ja` + `x-default` 四个 hreflang（已在 `CommonMeta.astro` 用 `LOCALES.map` 循环生成；新增页面如果走 PageLayout 自动继承，无需重复配置）。

## 5. 验证

### 5.1 自动检查（每次构建）

```bash
npm run build
npm run check:dist               # = check:i18n + check:schema
npm run check:i18n               # 默认扫 dist/en 中文残留
npm run check:i18n -- --lang ja  # 扫 dist/ja 中文残留
```

脚本逻辑：
1. 走 `dist/<lang>/**/*.html`
2. 剥离 `<script>` `<style>` `<head>` 与所有 HTML 属性
3. 按 `LANG_CONFIG[<lang>].foreignRegex` 搜索"不该出现的脚本"（EN 默认是 CJK Unified Ideographs）
4. 对照 `LANG_CONFIG[<lang>].allowPatterns` 白名单
5. 命中即报错并定位到具体页面

EN 当前白名单（每条都有理由）：
- `中文版可用` / `阅读中文版` / `中`：LangBanner 与 LanguageToggle 邀请用户切换到 zh 的副本。

如果你为某条特殊残留申请例外，**必须**在 `LANG_CONFIG[<lang>].allowPatterns` 里加注释说明原因。

### 5.2 反向检查（zh 页面是否被英文污染）

目前没有自动化的"zh 页面英文残留"检查，因为 zh 页面允许大量英文（人名、机构缩写、产品名）。如果未来需要，可以加一个 `zh-check.mjs` 反向版，但允许列表会很长，价值有限。手工 review 即可。

### 5.3 PR / 提交前必跑

```bash
npm run check          # astro check + eslint + prettier + verify-graph
npm run build          # 必须先构建才能跑 i18n-check / check-schema
npm run check:dist     # = check:i18n + check:schema（中文残留 + JSON-LD）
```

## 6. 新增内容时的清单

### 6.1 新增数据条目（debate / policy / lever / video / 等）

- [ ] zh 字段填全
- [ ] 每个用户可见的 zh 字段都加了 `*En` 和 `*Ja` 兄弟字段（标题、摘要、要点、状态描述、机构名）
- [ ] 数组字段同样加了 `*En` / `*Ja` 数组（即使内容相同也要复制一份，防止页面在 EN/JA 下显示 zh）
- [ ] 新增 / 更新国会辩论后运行 `npm run fetch:debate-transcripts && npm run translate:debate-transcripts && npm run check:debate-transcripts`
- [ ] 跑一次 `npm run build && npm run check:dist` 看新条目是否引入新残留

### 6.2 新增页面

- [ ] `src/pages/<route>/...astro`（zh）、`src/pages/en/<route>/...astro`（EN）、`src/pages/ja/<route>/...astro`（JA）三份
- [ ] EN 页面顶部 `const lang = 'en' as const;`，JA 页面顶部 `const lang = 'ja' as const;`，传给 Layout
- [ ] 渲染数据走 `pickLocalized(record, baseKey, lang)`
- [ ] 内部链接用 `localizedHref`
- [ ] 跑 `npm run build && npm run check:dist` 通过

### 6.3 改动共享组件

- [ ] 用 `getLangFromPath(new URL(Astro.url).pathname)` 推断 lang
- [ ] 显示文案用 `t(lang, key)` 或本地三元
- [ ] 字段取值用 `pickLocalized`
- [ ] 链接用 `localizedHref`
- [ ] zh 与 EN 各跑一遍构建确认两端都对

### 6.4 SEO 调优

- [ ] 修改 `<title>` / description / og 标签时同时改 zh 与 EN 两份字典
- [ ] hreflang 必须同时存在（zh ↔ en 双向）
- [ ] 页面 metadata 不要硬编码"新加坡 AI 观察"或"Singapore AI Observatory"，统一走 `t(lang, 'siteName')`

## 7. 已知限制

- `src/data/post/en/*.md` 和 `src/data/post/ja/*.md` 的博客以独立文件存在，与 zh 版手工保持一致；改 zh 文章不会自动同步到 EN/JA，需要三改。
- `LangBanner` 在 EN/JA 页面显示中文邀请文案（`中文版可用`、`阅读中文版`），是有意为之——这是给具有中文浏览偏好的访客看的。i18n-check 已为此加了白名单。
- Hansard 国会辩论的"speakers"字段保持英文姓名（与 Hansard 原文一致）。
- JA 翻译使用 です・ます 调（专业 web 站通用敬语等级）。
- 日语术语表：「シンガポール AI 観測」（站名）、「AI レバー」（抓手）、「議会討論」（辩论）、「政策文書」（政策）、「エコシステムマップ」（生态）、「タイムライン」（时间线）、「ベンチマーク」（对标）。

## 9. 数据层批量翻译（ko 回填）

ko UI 字典已 seed，但数据文件里的 `*Ko` 字段尚未填充。一次性脚本（按域跑）：

```bash
# 模板（基于 scripts/i18n/build-ja-dict.ts 的批量回填模式）：
# - 读 src/data/<domain>.ts
# - 对每条 record 的 title / description / summary 等 CJK 字段
# - 调 translateBatch({ direction: 'zh→ko' })，写 titleKo / descriptionKo
# - sha256 缓存（重跑零成本）
# - 写回 .ts 文件

# 建议按这个顺序回填（高曝光优先）：
# 1. policies.ts        — 约 80 条
# 2. levers.ts          — 约 30 条
# 3. debates.ts         — 约 100+ 条（content 长）
# 4. ecosystem.ts       — 约 60 条
# 5. timeline.ts        — 约 50 条
# 6. people.ts          — 约 40 条
# 7. videos.ts          — 约 60 条
# 8. tracker.ts         — 约 40 条
# 9. talent.ts          — 约 30 条
# 10. startups.ts       — 约 50 条（含 autoDiscovered[]）
# 11. video-transcripts.ts — 视频字幕，量大（看 fetch-transcripts.ts auto-chain）
```

跑完后 `npx tsx scripts/lib/i18n-pair.ts --locales=en,ja,ko src/data/*.ts` 验证字段配对。i18n-pair 默认 gate 是 `['en']`，加 ja/ko 是 opt-in。

**Refresh 管线 ko 集成**：当前 11 条 refresh 管线（policies / ecosystem / levers / legal-ai / talent / startups / tracker / benchmarking / videos / voices / hansard）只产出 `*En` + `*Ja`。让它们自动也产出 `*Ko`：在每个 `emit.ts` 找到 `translateRecords(records, [...], { direction: 'zh→ja', targetSuffix: 'Ja' })` 调用，旁边并列加一行 `direction: 'zh→ko', targetSuffix: 'Ko'`。

## 10. 历史改动记录

- **2026-05-23 v0.13.0 — 五语（zh + en + ja + zh-tw + ko）基础设施**：i18n core 泛化（`Lang` 加 `'zh-tw' | 'ko'`，`siblingSuffix` 支持 hyphen-split，`FALLBACK_CHAINS` 加 zh-tw/ko 链，OpenCC s2twp runtime 转换接入 `pickLocalized` + `t()`），280 key ko 字典 seed（[build-ko-dict.ts](../scripts/i18n/build-ko-dict.ts)，haiku 通过 zh→ko），OpenCC singleton 在 [src/i18n/opencc.ts](../src/i18n/opencc.ts)（cn→twp + 20 条 post-process 补丁），`opencc-js@1.3.1` 加入依赖，Layout/CommonMeta/LanguageToggle/LangBanner 5 locale 适配，`translate.ts` 加 6 个 ko-方向（zh↔ko / en↔ko / ja↔ko），`i18n-check.mjs` LANG_CONFIG 加 zh-tw + ko 入口，`astro.config.ts` sitemap 自动产出 5 个 hreflang alternate + x-default，1028 个 `/zh-tw/` HTML + 1028 个 `/ko/` HTML 全部 build 通过 `check:dist`。zh-tw 不需要数据字段（OpenCC 运行时转换）；ko 数据层 `*Ko` 回填待办（第 9 节）。

- **2026-05-10 v0.16.0 — ja 纯度根因修复 + Layer D/E 加固**：用户审计发现 ja 页面大量保留中英文。根因有三：
  1. `pickLocalized` 4-参 shape B 实现 ja 永远拿 enKey、不查 *Ja —— 50 处调用全部受影响（[修复](../src/i18n/index.ts#L127)）。
  2. 25 个 .astro 模板有 `lang === 'zh' ? '中' : 'EN'` / `isZh ? '中' : 'EN'` 二元三元（518 处），ja 静默落到 EN。
  3. Layer D 只检测 ja 页面里的中文残留（CJK），完全不检测英文残留 —— eval 报 0 但实际数千处。
  修复：i18n core shape B 修；EvolutionPage / TimelinePage / AboutPage 的 COPY 字典补 ja；DebatesIndex 7 个 helper 函数从 isEn 二元改三元链；ecosystem/index、videos/[id]、voices/[id] 高曝光页面去 hardcode；i18n-check.mjs 加 EN sentence scan + `data-i18n-allow-en` marker；Layer E 源码层硬编码扫描（baseline 506）+ CI hard-fail 新增。详见 [docs/20260510-ja-i18n-purity-fix.md](20260510-ja-i18n-purity-fix.md)。
  防再发生范式：**任何含 lang 的 ternary 必须三语都覆盖**，写法 `isJa ? '...' : isEn ? '...' : '...'`，或者用 `t(lang, key)` / `pickLocalized(record, 'field', lang)`。Layer E 在 PR 时强制 catch。
- **2026-05-08 v0.12.0 — 三语（zh + en + ja）全站上线**：i18n core 泛化（`Lang` 加 `'ja'`，`FALLBACK_CHAINS` 加 `ja: ['ja', 'en', 'zh']`，`t()` 改字典 map 查表），~190 key 的 ja 字典，12 个 `src/data/*.ts` 共 ~5,756 条 `*Ja` 字段回填，8 篇日语博客 `src/data/post/ja/`，42 个 `src/pages/ja/` 页面镜像（~1,048 页 HTML），11 条 refresh 管线自动产出 ja 翻译（run-template.ts + policies + ecosystem + levers + legal-ai + videos 全部 wired），`auto-discovered-emit.ts` 的 `AutoDiscoveredEntry` 加 `titleJa` / `descriptionJa`，`BilingualSummary` 加 `titleJa` / `descriptionJa`，`i18n-check.mjs` 支持 `--lang ja`，`CommonMeta.astro` hreflang 改 `LOCALES.map` 循环。
- **2026-05-03 v0.6.0 — 镜像页 thin wrapper 化**：发现首页 zh/en 漂移两次设计迭代（`9220401` "Simplify homepage"、`429ba17` "polish editorial layout" 都没同步到 EN）。根因是 page 文件双副本独立维护、没有结构约束。引入新规则 §3.4：镜像页结构唯一存在于 `src/components/<area>/<PageName>.astro`，page 文件降级为 metadata + JSON-LD + 单一组件渲染。首页率先迁移：[`HomePage.astro`](../src/components/home/HomePage.astro) ~280 行 + zh/en 包装各 ~40 行，替代之前的 zh 306 + en 418 双副本。`closingThesis` / `closingCta` 字典更新到当前文案，新增 `homeTrackerSection` / `homeTrackerBlurb` / `homeTrackerCta` 三个 key。后续 about、timeline 等镜像页按同样模式迁移。
- **2026-05-02 v0.5.0 — i18n 多语言化收尾**：撤销 zhName/zhTitle 在 EN 页面的副显示（包括 person 卡片、MP profile、policy zh title、debate zhTitle）。补 `LegalItem.dateEn`、`LeverItem.ministryEn`、`SocialChannel.labelEn` 等剩余的 missing siblings。`pickLocalized` 升级为多语言通用形态（`pickLocalized(record, baseKey, lang)`），按 lang 自动算 sibling 字段名（`title` / `titleEn` / `titleJa` / ...）；旧 4-参形态保留兼容。`getLangFromPath` / `localizedHref` / `unprefixed` / `localePrefix` 全部 N-locale 化，加新语言只需扩 `LOCALES` + `FALLBACK_CHAINS`。`scripts/i18n-check.mjs` 接受 `--lang` 参数。
- **2026-05-02 v0.4.0 — i18n 大整顿**：引入 `pickLocalized` / `channelLabel` / 共享 helper，覆盖修复 366 个 EN 页面共 1970 处中文残留。新增 `scripts/i18n-check.mjs` 作为构建验证。Footer / Metadata 现按 lang 输出 `siteName`、`og:locale`、`title` 模板。AuthorBio / TableOfContents / NextPrevPost / RelatedRail / blog ListItem / SinglePost 全部 lang-aware。`getRelatedPosts` 限定同语言邻居。新增 `src/data` 数据接口的 `*En` 兄弟字段约定固化。
- 更早的 i18n 演进见 CHANGELOG。
